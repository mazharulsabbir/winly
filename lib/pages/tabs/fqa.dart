import 'package:flutter/material.dart';

class FQATab extends StatelessWidget {
  const FQATab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Friquent asked questions'),
    );
  }
}
